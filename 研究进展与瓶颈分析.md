# 融合长期记忆与层次化任务规划的具身智能 AgentOS：研究进展与瓶颈分析

## 一、课题定位

拟定题目：

> 融合长期记忆与层次化任务规划的具身智能 AgentOS 设计与实现

本课题关注的核心问题不是机器人硬件控制，也不是简单的大模型问答系统，而是研究具身智能 AgentOS 中长期记忆与任务规划的协同机制。

核心目标可以概括为：

```text
长期记忆
  -> 任务上下文构建
  -> 高层任务分解
  -> 低层技能规划
  -> 可执行性校验
  -> 技能执行
  -> 执行反馈
  -> 记忆更新
```

系统需要证明：具身智能终端能够基于历史交互、用户偏好、任务经验和执行结果，生成更加连续、个性化、可执行的任务计划。

## 二、公开研究已经达到的层次

### 2.1 LLM Agent 长期记忆机制已经比较成熟

代表研究：

- Generative Agents: Interactive Simulacra of Human Behavior
- A Survey on the Memory Mechanism of LLM-based Agents
- LoCoMo: Evaluating Very Long-Term Conversational Memory of LLM Agents
- A-MEM: Agentic Memory for LLM Agents
- Mem0: Building Production-Ready AI Agents with Scalable Long-Term Memory

这些研究已经解决的问题：

- Agent 需要长期记忆，不能只依赖上下文窗口。
- 记忆可以按照 observation、reflection、plan、profile、event 等不同形式组织。
- 长期记忆系统需要具备写入、检索、压缩、总结和更新能力。
- RAG 和长上下文都可以提升长期对话表现。
- 记忆管理已经开始从简单向量检索转向结构化、图结构和动态组织。

暴露的问题：

- 普通向量检索容易召回噪声记忆。
- 长期记忆容易过时、冲突、重复。
- 多轮、多事件、多时间线的记忆推理仍然较弱。
- 长期记忆不等于长期理解，模型在时间关系、因果关系和跨会话一致性方面仍有明显不足。

对本课题的启发：

不能只做“聊天记录 + 向量库”。长期记忆应当服务于任务规划，需要包含用户、任务、场景、执行结果和反馈等结构化信息。

## 三、机器人任务规划的研究进展

### 3.1 从文本规划走向可执行规划

代表研究：

- SayCan: Do As I Can, Not As I Say
- Inner Monologue: Embodied Reasoning through Planning with Language Models
- DELTA: Decomposed Efficient Long-Term Robot Task Planning using Large Language Models

这些研究已经解决的问题：

- 大模型可以理解高层自然语言目标。
- 机器人不能直接执行大模型生成的自然语言计划，必须结合可用技能和环境约束。
- SayCan 将语言模型的任务判断与机器人技能 affordance 结合，用于判断“应该做什么”和“能不能做”。
- Inner Monologue 引入环境反馈、成功检测和人类反馈，使机器人能够在执行过程中调整计划。
- DELTA 将长期任务拆分为子目标，并结合场景图和自动规划器，提高长期任务规划效率。

暴露的问题：

- 大模型生成的计划经常看起来合理，但实际不可执行。
- 长任务拆解容易出现错误传播，高层子目标错误会导致低层技能序列错误。
- 许多方法偏向当前任务和当前环境，对长期用户记忆利用不足。
- 层次化任务规划与长期记忆之间缺少稳定结合机制。

对本课题的启发：

本课题中的任务规划不能只是大模型直接输出步骤，而应设计为层次化结构：

```text
用户目标
  -> 高层子任务规划
  -> 低层技能规划
  -> Skill Library 约束
  -> 可执行性校验
```

## 四、具身记忆的研究进展

### 4.1 从文本记忆走向空间、场景和任务记忆

代表研究：

- Embodied-RAG: General Non-parametric Embodied Memory for Retrieval and Generation
- KARMA: Augmenting Embodied AI Agents with Long-and-short Term Memory Systems

这些研究已经解决的问题：

- 具身智能的记忆不是普通文本记忆，而是多模态、空间化、任务相关的记忆。
- Embodied-RAG 强调具身智能需要层次化语义记忆来支持导航和问答。
- KARMA 将长期记忆和短期记忆结合，长期记忆保存场景图，短期记忆记录物体状态变化，用于家庭任务规划。

暴露的问题：

- 具身记忆很难统一表示，空间、物体、用户、动作、时间和结果都需要被组织。
- 环境变化后，旧记忆可能误导任务规划。
- 很多研究偏环境记忆，对用户偏好、任务历史和执行反馈覆盖不足。
- 真实机器人部署比仿真环境复杂，记忆更新和错误修正难度更高。

对本课题的启发：

本课题应避免只做用户画像记忆，长期记忆至少应包含：

```text
用户记忆
任务记忆
场景记忆
执行反馈记忆
```

其中，任务记忆和执行反馈记忆是连接长期记忆与层次化任务规划的关键。

## 五、记忆增强具身规划的最新进展

### 5.1 研究已经开始接近本课题方向

代表研究：

- LLM-Empowered Embodied Agent for Memory-Augmented Task Planning in Household Robotics
- MEMENTO: Embodied Agents Meet Personalization

这些研究已经解决的问题：

- 将 RAG 记忆用于家庭机器人任务规划。
- 通过多 Agent 分工实现任务路由、任务规划和知识库管理。
- 让机器人能够追踪过去动作和环境变化，支持用户基于历史的追问。
- 评估具身智能体是否能利用用户历史记忆完成个性化协助。

暴露的问题：

- 当任务需要同时引用多条记忆时，模型性能明显下降。
- 模型较容易处理单条个性化记忆，但对连续习惯、用户模式和多记忆联合推理能力不足。
- 记忆越多不一定越好，可能造成信息过载和规划干扰。
- 当前系统多是原型级，缺少统一的 AgentOS 式模块架构。

对本课题的启发：

本课题可以进一步研究：

```text
长期记忆如何选择
长期记忆如何转化为规划依据
长期记忆如何影响高层子任务
长期记忆如何影响低层技能选择
执行反馈如何反向更新记忆
```

## 六、已有研究整体解决了什么

综合来看，公开研究已经推进了四类问题。

### 6.1 LLM Agent 需要长期记忆

这已经不是争议点。研究重点已经从“要不要记忆”转向“如何组织、如何更新、如何检索和如何遗忘”。

### 6.2 机器人规划必须受技能和环境约束

大模型不能直接控制机器人。任务规划必须受到 Skill Library、环境状态、affordance 和安全规则限制。

### 6.3 长期任务需要层次化拆解

长期任务不能一次性生成平铺步骤，而需要拆成高层子目标和低层技能动作。

### 6.4 具身记忆不同于普通文本 RAG

具身智能需要处理空间、物体、用户、状态变化、任务结果和个性化语义，因此普通文本 RAG 不足以支撑完整任务规划。

## 七、当前方向暴露出的深层次问题

### 7.1 记忆表示问题

现有系统常把记忆做成文本片段或向量，但具身任务需要更丰富的表示方式。

面向具身任务的记忆应至少回答：

```text
谁在什么场景下提出了什么目标？
系统调用了哪些技能？
任务是否完成？
失败原因是什么？
用户反馈如何？
下次规划应该如何调整？
```

因此，本课题可以设计面向具身任务的情景记忆结构。

### 7.2 记忆选择问题

记忆不是越多越好。关键问题是：

```text
当前任务需要哪几条记忆？
哪些记忆已经过时？
哪些记忆互相冲突？
哪些记忆会干扰规划？
```

如果没有记忆选择和过滤机制，Planner 很容易被无关记忆误导。

### 7.3 记忆到规划的转化问题

很多系统能检索记忆，但没有清楚说明检索出的记忆如何影响任务规划。

本课题需要重点解决：

```text
记忆如何影响高层子任务？
记忆如何影响低层技能选择？
记忆如何影响技能参数？
记忆如何影响失败恢复策略？
```

这也是“融合长期记忆与层次化任务规划”的核心。

### 7.4 计划可执行性问题

大模型生成计划最大的问题是不可执行。解决这个问题不能只靠 prompt，而要依赖系统约束。

需要设计：

- Skill Registry
- 技能输入输出约束
- 技能前置条件
- 技能后置效果
- 安全约束
- 执行状态反馈

这样才能保证任务计划能够被 AgentOS 执行。

### 7.5 执行反馈闭环问题

许多研究停留在：

```text
检索记忆 -> 生成计划 -> 执行
```

更深层的问题是：

```text
执行结果 -> 更新记忆 -> 改变下一次规划
```

如果系统能够证明执行结果会影响后续规划，论文深度会明显提高。

### 7.6 评测问题

当前许多系统演示性较强，但缺少统一评测。对于本课题，可以设计三组对比：

```text
A 组：无记忆 + 单层规划
B 组：普通向量记忆 + 单层规划
C 组：分层长期记忆 + 层次化任务规划
```

可选指标：

- 记忆命中率
- 计划可执行率
- 技能调用成功率
- 个性化匹配率
- 任务完成率
- 多轮连续任务一致性
- 平均规划时间

## 八、本课题的研究空间

本课题可以建立在已有研究基础上，聚焦以下研究空白：

> 现有研究分别探索了长期记忆、具身任务规划和技能执行，但长期记忆如何稳定地支持层次化具身任务规划，并通过执行反馈持续更新，仍然缺少清晰的系统化实现与评测。

因此，本课题可以形成如下研究路线：

```text
分层长期记忆模型
  -> 任务相关记忆选择
  -> 高层子任务规划
  -> 低层 Skill 规划
  -> 计划可执行性校验
  -> 技能执行
  -> 执行反馈写入
  -> 后续规划自适应
```

## 九、对论文题目的实现要求

为了避免题目落成普通应用系统，论文实现应避免以下简单路径：

```text
用户输入 -> RAG 检索 -> LLM 生成步骤
```

更合理的实现路径应为：

```text
情景长期记忆模型
  -> 相关记忆选择与过滤
  -> 高层子任务规划
  -> 低层 Skill 规划
  -> 可执行性校验
  -> 执行反馈
  -> 记忆更新
```

最终系统应体现三个核心贡献：

1. 面向具身智能任务的长期记忆组织机制。
2. 长期记忆增强的层次化任务规划机制。
3. 基于执行反馈的记忆更新与规划自适应闭环。

## 十、可放入开题报告的总结表述

可以在开题报告中这样表述研究背景和问题：

> 当前大模型 Agent 在长期记忆、任务规划和工具调用方面已经取得进展，但具身智能场景对记忆组织和任务规划提出了更高要求。具身智能终端不仅需要记住用户偏好，还需要记住任务历史、场景状态和执行反馈，并将这些记忆转化为可执行的层次化任务计划。现有研究在记忆表示、记忆选择、计划可执行性和反馈更新方面仍存在不足。本文拟设计并实现一种融合长期记忆与层次化任务规划的具身智能 AgentOS 原型系统，通过分层记忆模型、层次化 Planner、Skill Library 和执行反馈机制，实现从记忆检索到任务执行再到记忆更新的闭环。

## 十一、参考论文线索

后续文献综述可优先围绕以下论文展开：

- Park et al., Generative Agents: Interactive Simulacra of Human Behavior.
- Wang et al., A Survey on the Memory Mechanism of LLM-based Agents.
- Maharana et al., Evaluating Very Long-Term Conversational Memory of LLM Agents.
- Xu et al., A-MEM: Agentic Memory for LLM Agents.
- Mem0: Building Production-Ready AI Agents with Scalable Long-Term Memory.
- Ahn et al., Do As I Can, Not As I Say: Grounding Language in Robotic Affordances.
- Huang et al., Inner Monologue: Embodied Reasoning through Planning with Language Models.
- DELTA: Decomposed Efficient Long-Term Robot Task Planning using Large Language Models.
- Embodied-RAG: General Non-parametric Embodied Memory for Retrieval and Generation.
- KARMA: Augmenting Embodied AI Agents with Long-and-short Term Memory Systems.
- LLM-Empowered Embodied Agent for Memory-Augmented Task Planning in Household Robotics.
- MEMENTO: Embodied Agents Meet Personalization.
